# FEMNIST Federated Learning Configuration

experiment:
  id: "experiment_1"
  save_dir: "./results/femnist"

training:
  T: 3500                      # Number of training rounds
  lr: 0.06                   # Learning rate
  local_steps: 3             # Number of local steps per round
  batch_size: 32          # For Shakespeare
  eval_batch_size: 64    # For Shakespeare

clients:
  n_players: 10              # Number of clients per round
  alpha_0: 1.0               # Scaling factor for good clients
  alpha_1: 1.0               # Scaling factor for bad clients
  beta_0: 0.0                # Noise level for good clients
  beta_1: 0.0                # Noise level for bad clients

aggregation:
  method: "mean" # "weighted_average", "median", "trimmed_mean"

data:
  train_path: "./data/femnist/train.json"
  test_path: "./data/femnist/test.json"